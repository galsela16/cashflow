# CashflowHQ — כיוון פיתוח

## מטרת המוצר

לאפשר להבין בתוך פחות מעשר שניות:

1. כמה כסף זמין עכשיו.
2. כמה צפוי להיכנס ולצאת בקרוב.
3. מה דורש טיפול היום.
4. מאיפה הגיע השינוי לעומת החודש הקודם.

## המצב הנוכחי

האפליקציה כוללת יכולות רבות ושימושיות: תזרים בית ועסק, עסקאות, לקוחות, עובדים, אירועים, חשבונות, תחזית, יעדי חיסכון, Profit First, התראות ויועץ. עם זאת, קוד האפליקציה עדיין מרוכז בקובץ `app.js` גדול עם מצב גלובלי ורינדור ישיר של מסכים רבים. המבנה עובד, אך מקשה לפתח ולבדוק שינוי בלי להשפיע על אזורים אחרים.

## סדר העדיפויות המומלץ

### 1. לוח מחוונים ממוקד החלטות

- שורת מצב אחת: זמין עכשיו, צפוי בסוף החודש ותזרים 30 הימים הקרובים.
- אזור "דורש טיפול": חובות לקוחות, תשלומים קרובים, חריגה והפרשי חשבונות.
- השוואה לחודש קודם והסבר קצר לכל מספר.
- פרטים משניים יוצגו בהרחבה לפי דרישה ולא כולם בבת אחת.

### 2. מקור אמת אחיד למספרים

- לרכז את חישובי ההכנסות, ההוצאות, החובות והתחזית בפונקציות טהורות.
- להציג בכל נתון אם הוא בפועל, צפוי או משוער.
- להוסיף זמן עדכון אחרון ומצב סנכרון.

### 3. פיצול הדרגתי של הקוד

- `core/` — מצב האפליקציה, תאריכים, פורמט וחישובים.
- `data/` — כל הגישה ל-Supabase.
- `features/` — דשבורד, עסקאות, לקוחות, עובדים, חשבונות ויעדים.
- `ui/` — רכיבים משותפים, הודעות, חלונות וטבלאות.

הפיצול ייעשה מסך אחר מסך, בלי שכתוב מלא ובלי שינוי בסיס הנתונים בשלב הראשון.

### 4. אמינות לפני תכונות חדשות

- בדיקות אוטומטיות לחישובים הפיננסיים המרכזיים.
- מצבי טעינה, שגיאה וריק ברורים.
- מניעת טעינות כפולות ובקשות Supabase מיותרות.
- בדיקת הרשאות RLS, גיבויים ושחזור נתונים.

## הצעד הבא המומלץ

להתחיל בדשבורד העסקי: להגדיר יחד את ארבעת המספרים החשובים ביותר ואת רשימת הפעולות היומית, ואז לבנות אותם מעל שכבת חישובים אחידה. לאחר שהדשבורד החדש יציב, להשתמש באותו מודל גם במצב הבית.
