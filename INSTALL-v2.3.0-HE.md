# התקנת CashflowHQ v2.3.0

## פריסה בצעד אחד

החליפו ב-GitHub את קובצי האתר בתוכן התיקייה הזו ובצעו Deploy אחד ב-Vercel.

אין צורך להריץ SQL, לשנות משתני סביבה או לעדכן את מבנה Supabase. גרסת 2.3.0 משתמשת באותו מסד נתונים של v2.2.1.

## מה השתנה

- דשבורד BUSINESS חדש שמציג מיד: זמין עכשיו, צפי לסוף החודש, תזרים ל-30 יום ומה דורש טיפול היום.
- פעולות פתוחות נגזרות מהנתונים הקיימים: תנועות שעבר זמנן, תשלומי לקוחות, שכר ממתין וחשבונות שלא עודכנו.
- פירוט ההכנסות, ההוצאות, העובדים והמס נשמר באזור נפתח בדשבורד.
- `Index.html` משתמש כעת בפועל ב-`styles.css` וב-`app.js`, כדי לאפשר טעינה ומטמון יעילים יותר.
- מטמון ה-PWA עודכן לגרסה חדשה כדי שהממשק החדש ייטען מיד לאחר הפריסה.

## לפני הפריסה

ודאו שכל הקבצים והתיקייה `supabase/` נמצאים יחד בשורש הפרויקט. אין למחוק את קובצי האייקונים, `manifest.webmanifest`, `pwa.js` או `sw.js`.
